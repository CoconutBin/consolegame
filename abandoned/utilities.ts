export const util = {
    length(x:any):number{
        if(typeof x == "number"){
        return x.toString().split('').length
        }
        else return x.length
    },
    forinc(){
        
    }
}
